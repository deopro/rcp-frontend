# RCP Frontend (Nuxt 4)

```bash
cd rcp-frontend
npm install
npm run dev
```

Open http://localhost:3000

PWA: installable in supported browsers; service worker enabled in development for validation.
